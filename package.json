{
  "name": "judge-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "turbo": "^2.7.3",
    "typescript": "^5.0.0"
  },
  "engines": {
    "npm": "Use pnpm",
    "yarn": "Use pnpm",
    "pnpm": ">=10.0.0"
  }
}
